# Contract Examples

Example payloads are organized by domain, then agent:

`contracts/examples/<domain>/<agent>/`

Each agent folder should include:
- `input.example.json`
- `output.example.json`
- Optional: `notes.md` for assumptions and edge cases
