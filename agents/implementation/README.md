# Implementation Agents

This group implements product code and data-layer changes.

## Agents
- `frontend_dev_agent`: Implements frontend features.
- `backend_dev_agent`: Implements backend endpoints and service logic.
- `db_schema_agent`: Owns schema lifecycle for bootstrap and evolve modes.
